using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ForestFireManager : MonoBehaviour
{
    void Update()
    {
        float dt = Time.deltaTime;
        Debug.Log("Delta time : " + dt);
    }
}
